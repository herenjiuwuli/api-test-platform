node_modules
dist
*.log

# 本地 SQLite 数据（运行时生成）
data/
.env
.env.*

# Playwright（M19）：报告与失败留痕都是产物，不进仓库
playwright-report/
test-results/
blob-report/
.e2e-tmp/

# 截图导览（M13）产物：给人看的图，不进仓库
# ⚠️ 刻意**不**放 test-results/：那边 Playwright 每次启动都会清空，图会被顺手删掉
shots/

# API 自动化测试平台 · 项目全讲

> 面向全栈 / 测试运维岗位的简历项目。一句话：一个能「存接口用例 → 手动/定时跑 → 出报告 → 带账号体系 → 一键部署」的自用接口监控与回归测试工具。
> 本文既是给面试官看的「项目讲解」，也可直接当作作品集博客发布。M1–M15 已完成，170 个自动化测试全绿，
> 并且**用这个平台把另一个自写项目 office-oa 测穿了**（60 条用例串成一条业务链，`npm run test:oa` → 60/60）。

---

## 1. 这个项目解决什么问题

实习时每天要手动点一堆接口，确认采集脚本没挂、返回格式没变。十几个接口一个个 Postman 点，既慢又容易漏。于是想做一个**自己的轻量版接口监控**：

- 把要盯的接口（URL + 期望结果）录进去；
- 点一下「全部运行」就挨个打过去，告诉我哪几个挂了、哪几个返回的字段不对；
- 还能设个 cron，每天定时自己跑，不用我动手；
- 跑完出报告，看通过率、看哪条接口最不靠谱。

和 Postman / JMeter 的区别：**零运维、零外部依赖、单文件数据库、自带账号体系和前端界面**，能直接 `docker compose up` 扔到服务器上给小团队用。

---

## 2. 架构

```
┌─────────────── 浏览器 (Vue3 + Element Plus) ───────────────┐
│  登录页 / 用例列表 / 请求编辑器 / 运行结果 / 报告+定时任务管理   │
└───────────────────────────┬────────────────────────────────┘
                            │  axios（自动带 Bearer token）
                            ▼
┌──────────── Fastify 后端（单端口 :3001）───────────────────┐
│  鉴权守卫 onRequest（Bearer 校验）                          │
│  /api/auth/*        注册/登录/me/改密                        │
│  /api/cases/*       用例 CRUD + 单条运行                    │
│  /api/run-all       全部运行汇总（支持按前缀只跑一组）        │
│  /api/schedules/*   定时任务 CRUD（node-cron）              │
│  /api/reports/*     报告聚合                                │
│  /api/environments/* 环境变量集 + 当前环境（M9）              │
│  /*                 生产环境同源托管 web/dist（SPA 回退）      │
│                                                            │
│  执行引擎 runner.js  →  发请求 + 多维度断言 + 变量渲染/抽取    │
│  请求体组装 bodyTypes/multipart/fixtures.js（M8）           │
│  变量链 vars.js + 环境注入 environments.js（M7/M9）          │
│  自写 JSONPath 求值器 jsonpath.js                           │
│  鉴权核心 auth.js    →  scrypt 哈希 + 手写 HS256 JWT         │
└───────────────────────────┬────────────────────────────────┘
                            ▼
                  node:sqlite（零原生依赖，data/app.db）
```

**关键取舍**：数据库用 Node 22 内置的 `node:sqlite`，不引任何原生编译模块（如 `better-sqlite3`），避免 CI / Docker / 不同平台上的编译坑。鉴权同样零依赖——密码哈希用内置 `crypto.scrypt`，Token 用手写 HS256 JWT，不引 `jsonwebtoken`。**multipart 包体也是手搓的**（M8），不引 `form-data` / `busboy`。

---

## 3. 技术亮点（面试可展开）

### 3.1 多维度断言 + 自写 JSONPath 求值器
断言模型 `expected`：
```json
{
  "status": 200,
  "contains": "ok",
  "maxTimeMs": 5000,
  "jsonChecks": [
    { "path": "$.data.total", "op": "gte", "value": 1 },
    { "path": "$.items[0].id", "op": "exists", "value": true }
  ]
}
```
支持：状态码、响应体包含、耗时上限、JSONPath 字段值（`eq/ne/gt/gte/lt/lte/contains/exists`）。

JSONPath 求值器**完全自写**，未用第三方库，支持 `$.a.b`、`$.arr[0]`、`$.arr[*]`、`.length`。核心逻辑（节选）：
```js
// src/jsonpath.js —— 极简路径求值，按 . 切分、[] 取下标、* 展开
function resolve(root, path) {
  const segs = path.replace(/^\$\.?/, '').split('.')
    .flatMap((s) => s.split('[').map((x) => x.replace(']', '')))
    .filter(Boolean)
  let cur = root
  for (const seg of segs) {
    if (cur == null) return undefined
    if (seg === '*' && Array.isArray(cur)) { cur = cur.flatMap((v) => Object.values(v)); continue }
    if (/^\d+$/.test(seg)) cur = cur[Number(seg)]
    else cur = cur[seg]
  }
  return cur
}
```
> 为什么自己写而不用 `jsonpath` 库？项目定位就是「手写轮子展示能力」，且第三方库对 `$.arr[*]`、`.length` 这类写法支持参差，自写 60 行更可控、更易测。

### 3.2 零依赖鉴权（scrypt + 手写 HS256 JWT）
密码哈希（防时序攻击）：
```js
import { scrypt, randomBytes, timingSafeEqual } from 'node:crypto'
function hashPassword(pw) {
  const salt = randomBytes(16).toString('hex')
  const derived = scrypt(pw, salt, 32).toString('hex')
  return `${salt}:${derived}` // 盐与密文同存
}
function verifyPassword(pw, stored) {
  const [salt, key] = stored.split(':')
  const derived = scrypt(pw, salt, 32)
  const keyBuf = Buffer.from(key, 'hex')
  return derived.length === keyBuf.length && timingSafeEqual(derived, keyBuf)
}
```
Token 用手写 HS256（base64url(header).base64url(payload).HMAC-SHA256），不引 `jsonwebtoken`。全局 `onRequest` 守卫统一拦截 `/api` 路由，放行 `/health`、登录、注册与静态资源。

### 3.3 测试隔离（一个真实踩过的坑）
早期测试把 `DB_PATH` 写成模块顶层 `const`：
```js
const DB_PATH = process.env.DB_PATH || 'data/app.db' // ❌ import 时就被快照
```
导致 `tests/setup.js` 里 `process.env.DB_PATH = ':memory:'` 在 import 之后才执行、**永不生效**，测试把用例写进了真实库——表现为「全部运行」出现一堆指向死链 `127.0.0.1:随机端口` 的 `fetch failed`。

修法：DB 路径改为 `getDb()` 内**惰性解析**，配合 `setupFiles` 在 import 前注入 `:memory:`，彻底隔离。这是测试可靠性的关键一役，也写进了 `TESTING.md`。

### 3.4 单端口部署
前端 `web/dist` 构建后由 Fastify 同源托管（`/*` 静态路由 + SPA 回退），部署只需一个端口、无跨域。提供 `Dockerfile`（node:22-alpine 多阶段）+ `docker-compose.yml`，挂卷 `/app/data` 持久化 SQLite。

### 3.5 用例链：让「需要登录的接口」真的能被自动测
最初的执行引擎一次只发一个请求 —— 登录拿到的 token 传不到下一个用例，于是**凡是需要鉴权的接口一条都测不了**。
补上的最小集合：

| 能力 | 做法 |
|---|---|
| 变量占位 | url / headers / body / 文件名里都能写 `{{token}}`，发请求前统一渲染 |
| 变量抽取 | 用例声明 `extract: [{ name: "rid", path: "$.id" }]`，从**本用例响应**里按 JSONPath 抽值 |
| 串链 | 「全部运行」按**创建顺序**跑，整轮共享一个变量袋（⚠️ 必须顺序，不能改成 `Promise.all`） |
| 分组 | `POST /api/run-all {"prefix":"OA-"}` 只跑一组 —— 平台里同时挂着打外网的示例用例，不分组会把别人的红算到自己头上 |

两个刻意的设计：
- **变量袋只在内存、不落库**：token 不该进数据库；也不该跨运行复用（否则会拿过期 token 假装通过）。
- **变量缺失不判失败**，但在 detail 里点名「未赋值变量：{{token}}」—— 缺变量时请求本来就会失败，两处都报会让人分不清真正原因。

配上一套真用例（office-oa 的 60 条）之后，平台终于能证明它是「能测真实系统的测试平台」，而不是「只能测自己 /health 的玩具」。

### 3.6 请求体类型与文件上传（手搓 multipart）
上一节补齐后还剩一条边界：执行器只会 `JSON.stringify(body)`，**发不出 `multipart/form-data`** ——
「上传文件」这件事平台压根做不到。这一条最初被我写进了「能力边界」，下一轮就当成工作项在**代码里**消掉了：

- `bodyTypes.js`：`json | raw | form-data` 白名单；`raw` 原样发字符串。
- `multipart.js`：手搓包体 —— `boundary` 生成、CRLF 与结尾 `--boundary--`、**二进制安全**、文件名字段引号转义 + 去换行。
- `fixtures.js`：内置夹具（PNG 41B / PDF 35B / 伪装 exe），**字节写进代码**而不是让用户传文件 ——
  自动化要的是「每次发一模一样的字节」，`size == 41` 这种断言才站得住。
- `runner.js`：form-data 时执行器**接管** `Content-Type`（带 boundary）与 `Content-Length`（用例手填的会被覆盖，大小写不敏感去重）。
- 夹具名写错**当场报错**，不静默发一个空文件 —— 那会让人以为「被测系统的上传接口有 bug」。

### 3.7 环境变量集：把「打谁」从用例里拿出来
原来被测地址只有 `BASE` 一个环境变量，而且**是在写用例时就被拼进 URL 的** ——
44 条 OA 用例的 url 里都烤着 `http://127.0.0.1:3200`，想打另一个环境就得重写整套用例。

改成：环境 = `{ name, baseUrl, headers, vars }`，用例里一律写 `{{base}}/api/xxx`，运行时按「当前环境」替换。
- **同一时刻只有一个当前环境**，存在 `settings` 表的**一行 KV** 里，而不是在 `environments` 上加 `is_active` 列 ——
  后者迟早会出现「两行都是 active」这种无法自证的状态。
- **优先级一：环境变量是基线，链上 `extract` 抽到的值优先。** 环境是「你去哪」，extract 是本次链上刚发生的事实；
  基线盖掉事实 = 用过期 token 假装通过。
- **优先级二：环境请求头是默认值，用例里手写的同名头优先**（大小写不敏感，否则请求里会同时出现 `Content-Type` 和 `content-type`）。
- `baseUrl` 保存时就归一化（去尾部斜杠 + 必须 http/https），把「地址写错」拦在保存时，而不是跑用例时给一句看不懂的 `fetch failed`。
- 三条运行路径（单跑 / run-all / 定时任务）都走 `runner.runCase`，环境只在**一处**生效，不会出现「手动跑用环境、定时跑不用」。

### 3.8 执行记录里的环境快照：配置是活的，历史是死的
3.7 把被测地址变成可配的，紧接着就冒出个新问题：报告里只有通过率，**答不出这次跑在哪个环境**。
地址写死的年代这不是问题（只有一个地址），地址一变可配，**每条不写清「当时用的是哪个值」的历史记录就都不可解释了**。

修法就一句：`runs` 表**存快照**，不只存外键。
- `env_name` + `base_url` 存的是**执行当时**的值；只存 `env_id` 的话，地址改一次（本地 → 预发），
  历史记录就被**反向改写**成「它从没打过的地址」。
- 报告新增「**按运行环境汇总**」，分组键就是这份快照 → 地址改过之后自然分成两组，「换个环境就全红」一眼可见；
  混在一起算平均值只会掩盖它。
- 老记录（升级前跑的）归到「(未记录环境)」一组，**不编造**环境名 —— 本机实测 557 条，
  这本身就是迁移的留痕。
- 这与 office-oa 的 `flow_snapshot`（改流程模板不影响在途单据）是**同一条教训**：**配置是活的，历史是死的。**

### 3.9 套件导出导入：让测试资产能带走
3.5–3.8 把平台做成了一个能跑 52 条用例链的闭环工具，但这些用例是**长在这个库里的** ——
想发给别人、想换台机器跑、想在 CI 上用，都只能重新 seed 一遍。**测试资产带不走，它就只是本地状态。**

于是加了 `GET /api/suite/export`（打成 JSON）与 `POST /api/suite/import`（吃回去）。三个设计点：

| 设计 | 为什么 |
|---|---|
| **不带 id** | id 是本地自增的，换库就指向别的东西；带过去只会让「按顺序串链」错位 |
| **导出顺序 = 串链顺序** | 用例链靠「前一条抽变量、后一条用变量」串起来。`listCases()` 是**倒序**的（列表页要新在前），直接导出会让导入后的链条**静默断掉** —— 用例还是一条条跑，只是全红，比报错难查得多。所以导出时显式按 id 升序排 |
| **敏感头脱敏，但按「值」判** | 导出文件天生要被分享（发同事 / 进仓库 / 贴 issue），`Authorization`、`Cookie` 的值抹成空串、键名保留并在文件里列出。判据是「键名敏感 **且** 值是字面量」：`X-Token: {{token}}` 是**变量引用**，抹了只会断链 |

导入侧有两条约定，都是「别一刀切」：
- **坏文件 400，坏条目继续**：单条用例缺 `url` 只记进 `failed`，其余照常导入。
  一个 51/52 的文件不该因为第 52 条被整份打回。
- **冲突默认 `rename`**（两个都留，新的加 `(2)`）：它既不悄悄丢你的数据，也不悄悄盖你的数据；
  `overwrite` / `skip` 是使用者显式选择的。

⭐ **这一节最值得讲的是那个「脱敏把变量引用也抹了」的缺陷**：它不是我推出来的，是端到端测试先红了才回头发现的
（详见踩坑 9）。**如果只写单元测试验证 `redactHeaders` 的输入输出，这个坑会一直藏着。**

### 3.10 用例分组：给「几十上百条用例」一个可读的节奏
M11 让用例能带走，但带走什么、跑什么，还是**一整坨**。52 条 OA 用例挂在列表里，只能靠名字前缀
（`OA-`）猜它讲什么；想「只跑附件那一组」也没有入口 —— 早先的实现是 `run-all` 支持一个 `prefix` 字符串，
那是分组的**最小实现**，连中文标签都装不下。

于是给 `test_cases` 加了一列 `group`（业务标签，**纯字符串，不建外键、不建枚举表**），落成四件事：

| 能力 | 实现 | 为什么这么设计 |
|---|---|---|
| **列表按组筛选** | `GET /api/cases?group=审批引擎`（`listCases(group)` 里加一个 `WHERE "group" = ?`） | 分组只是**展示与运行的视角**，不是实体；用一列字符串就够，硬上外键表是把「标签」做成了「分类体系」 |
| **按组运行** | `POST /api/run-all {group}`（沿用原来的 `runAll(cases)`，只在过滤那一步换谓词），返回里回显 `filter` | 与已有的 `prefix` / `ids` 过滤**同一入口**，不为「分组」另开一条运行路径（路径一多，环境注入这类「只在一处生效」的约束就会漏） |
| **报告按组汇总** | `/api/reports/summary` 新增 `byGroup`，与 `byEnv` 同一个聚合套路 | 通过率按**业务主题**切一刀，才能回答「是不是我这次改的东西把某一组全干红了」 |
| **套件往返保留分组** | 导出 JSON 带 `group`、导入写回 | 分组是**用例的属性**，导出不带就等于「带走一份丢结构的资产」 |

两个刻意的边界：**分组不改断言、不动串链顺序**（它是一层组织，不是一层逻辑，`run-all` 的过滤仍然
先把用例按 id 升序排好再筛）；**单个分组未必是自包含的链** —— 「审批引擎」那组依赖前面「登录」抽出的
token，单独跑会红。所以「按组运行」的价值是**按主题跑一坨**，不是替你切链；真要单跑某组，得把它需要的
前置变量在环境里备好。

### 3.11 删用例为什么要连带删「它产生的东西」
这是我在 M12 收尾时被自己绊到的一个坑，也是这个项目里**唯一一次「旧数据先骗了我」**：
写完分组的真机脚本要收尾清场，我发现删掉用例之后，**报告里那些「用例#id」还是赖着不走**。

一查才知道：`runs.case_id` 与 `schedules.case_id` **都不是外键**。于是三条删用例的路径全都只管删用例本身 ——
`deleteCase()`、`npm run seed`（示例用例）、`npm run seed:oa`（OA 套件）——
派生数据一律留成**孤儿**。而 `seed:oa` **每次都要先删旧用例再插新的**（否则重名、顺序乱），
所以每重新 seed 一次就多一批孤儿：**本机实测 791 条，涉及 433 个已不存在的用例 id**。
表现在界面上，就是报告页「按用例汇总」里一长串没有名字的「用例#id」，而且**只会越来越多**。

修法的关键不是「补一句 DELETE」，而是**收口**：把「删」这件事收到 `deleteCase` 一处，并在**同一事务**里做三件事：

| 连带删掉 | 为什么 |
|---|---|
| `runs` | 用例没了，它的执行历史就不可解释 —— 连「跑的是哪条用例」都答不出来，留着只是噪声 |
| `schedules` | 否则留下一个指向不存在用例的定时任务。⚠️ **删库行不会自动停内存里的 cron**，所以 `deleteCase` 要把被删的 `scheduleIds` **回传给路由**，由路由调 `refreshJob({id, enabled:false})` 停掉 |
| 用例本身 | 若用例不存在则**整笔回滚** —— 别顺手删掉恰好指向这个 id 的历史数据 |

两个 seed 脚本各自补上执行记录清理（顺序 `schedules` → `runs` → `cases`，后两步靠子查询找待删用例）。
接口把连带删掉的条数**如实回传**（`{deleted, runsDeleted, schedulesDeleted}`），前端提示
「已删除（连带 3 条执行记录）」—— **静默毁掉几十条历史，比多说一句话危险得多。**

历史遗留的孤儿用维护脚本扫尾（默认只报告，`--yes` 才删，删完复查「剩余 0 条」）：

```bash
npm run clean:orphans            # 只报告：多少条、涉及哪些已删除的用例 id
npm run clean:orphans -- --yes   # 真删（先 cp data/app.db data/backups/… 并验证备份可读）
```

> ⭐ 这条最值得讲的不是「我修了个 bug」，而是**「删实体」和「删它产生的影响」是两件事**。
> 任何「A 产生 B」的关系里（用例→执行记录、环境→历史记录、流程模板→在途单据），
> 只删 A 都会留下一堆解释不了 B。**问一句「它产生过什么」，比写十行 DELETE 更重要。**

### 3.12 响应头断言：给被测系统开了一面「导出」，工具就得多长一只眼

M14 我没往平台里加业务功能，是给 office-oa 开了一面新的 —— **单据导出 CSV**。结果这一面立刻把平台的
**第二处能力边界**顶了出来（第一处是 M8 的 multipart，在**请求侧**；这次在**响应侧**）：

执行器 `runCase` 从头到尾**只读 `res.text()`，从不碰 `res.headers`**。而「导出的对不对」有一半答案在头里 ——
`Content-Type` 是不是 `text/csv`、`Content-Disposition` 是不是 `attachment` —— 这些断言**一个字都写不出来**。

补了两处，都刻意走「复用」而不是「新造」：

| 改动 | 做法 | 为什么这么做 |
|---|---|---|
| **能读头** | 执行器把 `res.headers.entries()` 摊平成**小写键**对象放进结果；`expected.headers = [{name, op, value}]`，`op` **复用**已有的 `eq / contains / exists` | HTTP 头本就不区分大小写，摊平小写省掉一整个「大小写写错」的坑；操作符能复用就不新造一套 —— 少一套 DSL 就少一处要维护的语义 |
| **拿到字节** | 绕开 `res.text()`，按 `arrayBuffer()` 自己解码 UTF-8 | ⭐ 见下 —— BOM 会被 `res.text()` **静默吃掉**，不换路径根本断言不了 |

> ⭐ **第二处不是设计出来的，是跑测试时红出来的。** 我写了一条「响应体以 BOM 开头」的用例，真机上**永远红**，
> 回头才发现问题在 `res.text()`（Fetch 规范规定它会剥掉开头的 BOM），不在导出功能。
> 更阴的是它**静默**：不报错，只会一直红；**如果你把断言方向写反了，它甚至会假绿**。
> 这跟 3.6 的 multipart 是同一条路：**能力边界不是我设计出来的，是被一个真实需求逼出来的；
> 而消掉它的方式是在代码里补，不是在文档里改口。**

实测：`tests/headerAssert.test.js` 13 例（含「头名大小写不敏感」「头不存在时 `eq` 判失败」，以及那条
「`res.text()` 会吃掉 BOM」的回归）；闭环套件 I 段 8 条（OA-53～60）全用新断言跑通，`test:oa` 60/60。

---


## 4. 能跑什么（演示数据）
`npm run seed` 写入 5 条覆盖全能力的示例用例 + 1 条演示定时任务：

| 用例 | 展示能力 |
|---|---|
| 平台自检(/health) | 状态码 + 包含 + `$.ok eq true`（**离线必绿**） |
| GET + JSONPath | 多字段精确断言 |
| 数组断言 | `$.length gte 5` + `$.1.name exists` |
| POST + 请求体 | 带 body 发请求 + 校验回显字段 |
| 性能断言 | `maxTimeMs` 耗时上限 |

`npm run seed:oa` 另外写入 **60 条 office-oa 用例**（前缀 `OA-`，一条业务链 + 附件全生命周期 + 站内通知 + 单据导出），
它们是这个平台真正的「考卷」—— 详见 `docs/测穿-office-oa-闭环.md`。

---

## 5. 简历话术（可直接抄）

- **独立设计并实现一个 API 自动化测试平台**（Fastify + Vue3 + Node 内置 SQLite），覆盖用例管理、多维度断言（状态码 / 包含 / 耗时 / JSONPath / **响应头**）、定时任务、报告、账号体系、环境变量集、套件导出导入、用例分组与**删除的连带清理**、**分组定时任务（一个 cron 跑整组用例）**，已容器化部署，**170 个单元测试全绿**。
- 自研 **JSONPath 断言求值器**、**零依赖鉴权模块**（scrypt 哈希 + 手写 HS256 JWT）与 **multipart 包体生成器**，全程**不引入任何原生编译或第三方表单库**，保证跨平台与 CI 零编译坑。
- **实现用例链（变量渲染 + JSONPath 抽取 + 按创建顺序串链）**，使「需要登录的接口」也能被自动化覆盖；并用这套平台**把自己写的另一个系统 office-oa 测穿**（60 条用例串成一条业务链，60/60 通过）。
- **设计环境变量集**，把被测地址从 60 条用例里抽出来（用例写 `{{base}}`），并让「当前环境」在三处可见、一处可切，换环境不再需要重写用例；**执行记录改存环境快照**，使报告在环境地址变更后仍可解释（配置是活的，历史是死的）。
- 设计 **test/prod 数据库隔离方案**，修复了 ESM 模块顶层快照环境变量导致测试污染生产库的隐患。
- 实现 **单端口前后端同部署**，前端构建产物由后端同源托管，降低部署复杂度。

---

## 6. 踩坑记录（复盘价值）

1. **ESM 环境变量快照**：见 3.3，测试污染真实库。
2. **鉴权守卫误伤静态资源**：守卫最初只放行 `/health` 与登录/注册，导致生产环境前端路由（如 `/reports`）也被返回 401、登录页白屏。改为「非 `/api` 路径一律公开」并加回归测试。
3. **Windows 直接运行静默退出**：`import.meta.url === file://${process.argv[1]}` 在 Windows 反斜杠路径下恒假，改用 `pathToFileURL(process.argv[1]).href` 判断是否为直接运行。
4. **断言引擎的语义坑（拿真系统测出来的）**：用例要断言「权限数组里有 `user:read`」，写 `$.permissions[*]` + `contains` ——
   结果**假失败**，因为引擎的多匹配 `contains` 只看**第一个**匹配值（数组第一个是 `announcement:write`）。
   改成「**任一**命中即通过」，提示改成「匹配 N 个，首个 X」。⭐ **红的是我的工具，不是被测系统** —— 分开归因很重要。
5. **变量缺失的提示被异常分支吃掉了**：`{{base}}` 没赋值 → 渲染成空串 → url 变成 `/whoami` → `fetch` 直接抛错 → 走 catch 分支，
   而那段代码只写了 `detail: [e.message]`，**把「谁没赋值」这条最有用的线索丢在成功分支里了**。
   修法：把「未赋值变量」的计算提到发请求**之前**，catch 分支也带上它。⭐ **最需要提示的人，恰好是收不到提示的人。**
6. **一个会撒谎的环境徽标**：给页头加了「当前环境」徽标之后，真机点了一遍才发现 ——
   在环境页里点「取消当前环境」，**页头徽标纹丝不动**。原因是徽标画在 `App.vue`（外壳）、切环境发生在 `Environments.vue`（子页面），
   两处各读各的状态，靠「路由变化时重取」对齐；而**在同一页里切环境根本不引起路由变化**。
   修法：把当前环境抽成共享的 reactive 状态（`web/src/env.js`，与 `auth.js` 的 `session` 同一套路），谁改谁 set。
   ⭐ **一个会撒谎的状态显示，比没有这个显示更危险** —— 徽标写着「打测试环境」而平台其实在打别的地址，比不显示环境名更容易让人犯错。
7. **Vue 路由复用组件导致的表单串味**：从 `/cases/1/edit` 切到 `/cases/new`，vue-router **复用同一个组件实例**、`onMounted` 不重跑，
   于是新建页里残留着上一条用例的 `bodyType=form-data`，**凭空显示出「文件字段」**。
   修法：`resetForm()` + `watch(() => id, load)`。⭐ 这类 bug 单测抓不到、`vite build` 也不报错（Vue 对未声明标识符只 warning），**只有真机走一遍才露头**。
8. **把值从「固定」改成「可配」，顺带挖了个可观测性的坑**：见 3.8。地址写死时报告不需要说地址，
   一旦地址可切换，「不记录当时用的是哪个值」的历史就全部不可解释 —— 一个跨了两个环境的 92% 通过率，什么都不能说明。
   修法是把快照写进执行记录，而不是「显示得更清楚」。⭐ **配置是活的，历史是死的。**
9. **脱敏规则差点把变量引用也抹了（端到端测试先红的）**：给导出加「敏感头脱敏」时，第一版判据是
   「键名命中敏感名单就抹值」。跑端到端用例时发现 —— **导入回来看不见 token 了**：
   用例里的 `X-Token: {{token}}` 被当成凭据抹成空串，导入后第二条用例不带 token，靶子返回空串，断言红。
   ⭐ **`{{token}}` 不是藏在文件里的秘密，是运行期从链上取的占位符** —— 判据加上「值是字面量」这一条才对。
   教训：**单元测试只验证了「我以为的规则」，是这个「导出→清空库→导入→再跑一遍全绿」的端到端用例把规则本身问了出来。**
   （同类经验：白名单式的安全检查，先问「被它挡住的正常用法是什么」。）
10. **`DELETE` 带上 `Content-Type: application/json` 却没有 body → Fastify 直接 400**：
    真机验证脚本的收尾清理「一条也没删掉」，库里留了一堆残骸。原因是公共请求函数对所有请求都塞了
    `Content-Type: application/json`，而 Fastify 对「声明了 JSON 但 body 为空」的请求返回 `FST_ERR_CTP_EMPTY_JSON_BODY`。
    修法：只在**真的有 body** 时才带这个头。⭐ **收尾清理失败是静默的** —— 所以清理完必须再查一次「还剩几条」，
    而不是删完就宣布干净（顺带查出来：上一次跑挂掉的残骸 14 条也一起清掉了）。
11. **真机脚本导航到了不存在的路由**：验证脚本里写 `nav('/#/cases')`，页面看着正常（页头、导航都在），
    但按钮全找不到。真因：**这个平台的用例列表路由是 `/`**（`/cases/new`、`/cases/:id/edit` 才是编辑器），
    `/#/cases` 没有匹配项 → `router-view` 渲染空。⭐ **「页面看起来是好的」和「路由渲染成功」是两件事** ——
    诊断脚本一定要在失败时 dump 出 `location.href` + `script[src]` + 当前按钮列表，否则只能一直猜。
12. **只删了实体，没删「它产生的东西」**：删用例不连带删执行记录与定时任务（三个删除入口都没管），
    而 `seed:oa` 每次重跑都要先删旧用例 → 报告里积了 **791 条**没有名字的「用例#id」，而且只会越来越多。
    修法是把「删」收口到一处、在同一事务里连带清理，并把被删的 `scheduleIds` **回传给路由去停掉内存里的 cron**
    （⚠️ 删库行不会自动停任务）。⭐ **「删实体」和「删它产生的影响」是两件事** —— 详见 3.11。
13. **标准库的「贴心」清洗，会让字节级断言假绿**：给平台加响应头断言、想让 office-oa 的导出
    「必须带 UTF-8 BOM」也变成可断言项，结果这条用例**永远红**。查下来不是导出没写 BOM，而是
    **`res.text()` 按 Fetch 规范把开头的 BOM 吃掉了** —— 换 `arrayBuffer()` 自己解码才对。
    ⭐ 危险的不是「读不到」，是它**静默**：不报错，只是永远红（**判据写反了甚至会假绿**）。
    **凡是「验字节」的断言，先问一句：中间有没有哪个标准库替我做了一次我不想要的清洗？** —— 详见 3.12。

---

## 7. 怎么跑起来
```bash
npm install && cd web && npm install && cd ..
npm run dev        # 后端 :3001 + 前端 :5173
npm run seed       # 写入示例用例（首次打开就有东西可跑）
# 打开 http://localhost:5173 → 用 admin/admin123 登录 → 全部运行
npm test           # 170 例全绿

# —— 用这个平台去打 office-oa（闭环）——
cd ../office-oa && node seed.js --force && node index.js   # OA 在 3200 上跑
cd ../api-test-platform
npm run seed:oa    # 写入 60 条 OA 用例，并定义 + 选中「当前环境」
npm run test:oa    # 60/60；任一条失败以非 0 退出，可挂 CI
node scripts/m10-report-env-check.mjs   # 从报告接口读回「这一轮实际打的是哪个环境」
node scripts/m11-suite-ui-check.mjs     # 套件导出/导入（真机真的选文件导入，跑完自动清场）
node scripts/m12-group-ui-check.mjs     # 用例分组（真机真的选分组 + 点「运行该分组」，顺带验证删用例连带清执行记录）
npm run clean:orphans                   # 体检：报告里有没有指向已删除用例的执行记录（--yes 才真删）
```
部署：`docker compose up -d --build` → 访问 `http://<服务器IP>:3001`。
